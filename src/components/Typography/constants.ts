import {TypographyColors, TypographyFonts} from "./types.ts";

export const typographyColors: Record<TypographyColors, string> = {
    error: "#f00",
    main: "#333",
    white: "#fff"
}

export const fontVariants: Record<TypographyFonts, string> = {
    main: "16px",
    sm: "12px"
}